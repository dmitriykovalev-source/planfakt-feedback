# Журнал проекта «Сбор фидбека по план-факту»

## 2026-09-09 · Шаги 0–3 (частично)

**Где мы остановились:** архитектура записана в `02-архитектура.md`, но **не подтверждена**.
Следующее действие: HTML-разбор архитектуры «как для пятиклассника» + гейт на понимание
(Дмитрий пересказывает архитектуру своими словами, закрыв файл). Только после гейта — шаг 4
(секреты и запуск), затем шаг 5 (фазы и субагенты, `03-фазы-и-субагенты.md` + `план.html`).

**Что сделано**
- Шаг 0. Импакт посчитан. Порог выбран пользователем: *через месяц после релиза из фидбека
  собирается топ-5 проблем и уходит в спринт*. Порог не по количеству записей.
- Шаг 1. `01-импакт-и-идея.md`: граница v1, решения по контексту, 8 замороженных
  acceptance criteria проекта.
- Шаг 2. Гейт «демо качества» **не применяется**: в решении нет модели и нет выбора
  с качественной шкалой. Пропущен осознанно.
- Шаг 3. `git init` + `.gitignore` сделаны до первой строки кода. `02-архитектура.md` написан.

**Решения пользователя (не переигрывать без него)**
- Граница v1: форма + кабинет просмотра + CSV + скриншоты. Статусы, теги, комментарии,
  дашборд аналитики, редактор разделов, «свои обращения», встраиваемый виджет — следующий заход.
- Точка входа: отдельная страница по ссылке (не виджет в план-факте).
- Авторизация: Google OAuth, ограничение по корпоративному домену.
- Разделы: список в конфиге, правится в коде.
- Доступ в кабинет: список админов по email в конфиге.
- Скриншоты: до 3 файлов PNG/JPG, до 5 МБ каждый.
- Хранение файлов и базы: только на инфраструктуре компании, наружу ничего не выносим.
- Стек: TypeScript / React / Next.js. База: PostgreSQL.

**Контекст задачи**
Новый инструмент план-факта для HR, менеджеров и админов, 300+ пользователей, внутренний.
Сейчас сбора фидбека нет. Срок: один день до передачи разработке на код-ревью,
дальше тестировщики, дальше прод.

**Открытые вопросы — нужны от Дмитрия**
1. Список разделов план-факта (уйдёт в конфиг формы). Пока заглушка.
2. Корпоративный домен для ограничения OAuth.
3. Кто и как быстро создаёт OAuth-клиент в Google Workspace — единственный реальный риск
   для срока в один день.
4. Куда именно деплоим внутри контура.

**Что сломалось:** ничего.
